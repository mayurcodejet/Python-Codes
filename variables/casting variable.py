# so we have to cast the variable to diffrent it between in the other caterogry 
# so there are 3 types of the casting of the variable 
a = int(2) # this is int which is called as the integer casting which indicate the number   
b = str(3) # this is str which is called as the string in the variable 
c = float(3) # this is float variable which is show the varibale in the real number 
 
print(a)
print(b)
print(c)